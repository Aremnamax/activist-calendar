# Календарь мероприятий ПРОФ

Веб-приложение для централизованного планирования и управления мероприятиями университетской профсоюзной платформы «Личный кабинет активиста».

## Технологический стек

### Backend
- **NestJS** (Node.js/TypeScript) - модульный фреймворк с Dependency Injection
- **PostgreSQL** - реляционная база данных
- **TypeORM** - ORM для работы с БД
- **Socket.io** - WebSockets для real-time уведомлений
- **Nodemailer** - отправка email уведомлений
- **Telegraf** - Telegram Bot API для уведомлений
- **BullMQ** - очереди для фоновых задач

### Frontend
- **Next.js** (React/TypeScript) - SPA с SSR
- **Tailwind CSS** - стилизация
- **React Big Calendar** - календарь мероприятий
- **Zod** - валидация форм

## Структура проекта

```
.
├── backend/          # NestJS приложение
├── frontend/         # Next.js приложение
├── docker-compose.yml
└── README.md
```

## Быстрый старт

### Требования
- Node.js 18+
- PostgreSQL 14+
- npm или yarn

### Установка

```bash
# Установка всех зависимостей
npm run install:all

# Настройка переменных окружения
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# Запуск базы данных
docker-compose up -d postgres

# Запуск миграций
cd backend && npm run migration:run

# Запуск в режиме разработки
npm run dev:backend    # Backend на http://localhost:3000
npm run dev:frontend   # Frontend на http://localhost:3001
```

## Роли пользователей

- **Администратор** - одобряет/отклоняет мероприятия, видит все заявки
- **Организатор** - подаёт заявки на мероприятия, редактирует свои мероприятия
- **Активист** - просматривает календарь, подписывается на мероприятия

## Основной функционал

- 📅 Календарь мероприятий (месяц/неделя/день)
- 📝 Подача заявок на мероприятия
- ✅ Модерация заявок администраторами
- 🏷️ Фильтрация по меткам и подразделениям
- 🔔 Уведомления (email, Telegram, real-time)
- 📊 История изменений мероприятий
- 🔐 RBAC система доступа

## Подразделения

Система поддерживает 15 подразделений с уникальными цветами:
- ПРОФ.event (#ffe719)
- ПРОФ.life (#188184)
- ПРОФ.ГИ (#ec9a36)
- И другие...

## Лицензия

Проект разработан для университетской профсоюзной платформы ПРОФ.
